# 웨딩홀 빈자리 알림 — 사용설명서

> 개발을 모르셔도 따라 하실 수 있게 썼습니다. **전체 30분**, 그중 대부분은 설치를 기다리는 시간입니다.
> 한 번 끝내면 그 뒤로는 손 댈 일이 없습니다.

---

## 이게 뭘 해주나요

삼성금융연수원 예식장 예약 달력을 **10분마다 대신 새로고침**해 줍니다.
2027년 5·6·9·10·11월 중에 누가 예약을 취소해서 자리가 나면, **휴대폰으로 바로 알림**이 옵니다.

- 같은 날짜는 한 번만 알립니다 (같은 알림이 반복해서 오지 않습니다)
- 밤에도 새벽에도 계속 지켜봅니다
- **예약을 대신 잡아주지는 않습니다.** 알림을 받으면 직접 들어가서 예약하셔야 합니다

## 먼저 아셔야 할 것: 로그인은 꼭 필요합니다

결혼도움방은 삼성 임직원과 가족만 쓸 수 있는 회원 전용 사이트입니다.
예약 달력 자체가 로그인 뒤에 있어서, **로그인 없이 보는 방법은 없습니다.**

대신 이렇게 만들었습니다:

- 로그인은 **딱 한 번**, 설치할 때만 직접 하시면 됩니다
- 그 뒤에 로그인이 풀려도 프로그램이 **알아서 다시 로그인**합니다
- 그러니 실제로는 "처음 한 번 말고는 로그인할 일이 없다"에 가깝습니다

---

## 준비물

| | |
|---|---|
| 💻 | **켜 둘 수 있는 컴퓨터** — 노트북을 닫거나 끄면 감시도 멈춥니다 |
| 📱 | **휴대폰** — 알림을 받을 곳 |
| 🔑 | **결혼도움방 아이디/비밀번호** |

> 💡 노트북을 계속 켜두기 어렵다면, 나중에 한 달 몇 천 원짜리 클라우드 서버에 올리는 방법도 있습니다.
> 일단은 집 컴퓨터로 시작하셔도 충분합니다.

---

## 1단계 — 휴대폰에 알림 앱 설치 (2분)

**ntfy**(엔티에프와이)라는 무료 앱입니다. 앱스토어/플레이스토어에서 `ntfy` 로 검색하세요.

- [아이폰](https://apps.apple.com/app/ntfy/id1625396347)
- [안드로이드](https://play.google.com/store/apps/details?id=io.heckel.ntfy)

설치만 해두시고, **구독은 3단계에서** 하겠습니다. 거기서 이름을 알려드립니다.

## 2단계 — 프로그램 설치 (10분, 대부분 기다리는 시간)

**터미널**을 엽니다.

- **맥**: `⌘ + 스페이스` → `터미널` 입력 → Enter
- **윈도우**: 시작 버튼 → `PowerShell` 입력 → Enter

검은 창이 뜨면 아래를 **한 줄씩** 복사해서 붙여넣고 Enter 를 누르세요.
(붙여넣기는 맥은 `⌘V`, 윈도우는 마우스 오른쪽 클릭)

```bash
git clone https://github.com/hayaart/basic.git wedding-watch
cd wedding-watch
bash install.sh
```

> 설치하는 동안 글자가 주르륵 올라갑니다. **정상입니다.** 5~10분 걸립니다.
> `설치 완료!` 가 보이면 다음으로 넘어가세요.
>
> ❌ `git: command not found` 가 나오면 → 맥은 `xcode-select --install` 을 먼저 실행하세요.
> ❌ `python3: command not found` 가 나오면 → [python.org](https://www.python.org/downloads/) 에서 설치 후 터미널을 껐다 켜세요.

## 3단계 — 설정 (3분)

```bash
./.venv/bin/wedding-watch setup
```

질문이 하나씩 나옵니다. **대부분 그냥 Enter 만 누르시면 됩니다.**

| 질문 | 어떻게 답하나요 |
|---|---|
| 홀 이름 / 홀 코드 | 그냥 **Enter** (삼성금융연수원, 5 로 이미 맞춰져 있음) |
| 대상 월 | 그냥 **Enter** (2027년 5·6·9·10·11월). 바꾸려면 `2027-05, 2027-12` 처럼 |
| 주말만 볼까요? | 평일도 괜찮으면 **Enter**, 토·일만 원하면 `y` |
| 확인 주기 | 그냥 **Enter** (10분) |
| 토픽 이름 | 그냥 **Enter** ← 임의의 이름이 자동으로 만들어집니다 |
| 자동 로그인 | **Enter** 후 결혼도움방 아이디/비밀번호 입력 |

> 🔒 비밀번호는 **이 컴퓨터 안에만** 저장되고, 화면에 보이지 않으며, 인터넷 어디로도 전송되지 않습니다.
> 결혼도움방에 로그인할 때만 쓰입니다.

마지막에 이런 화면이 나옵니다:

```
   1. 휴대폰에 ntfy 앱을 설치하고 이 토픽을 구독하세요:
        wedding-7f08cd0e8b56          ← 이 이름
```

**👉 지금 휴대폰에서 ntfy 앱을 열고, `+` 버튼을 눌러 이 이름을 그대로 입력해 구독하세요.**

그리고 알림이 오는지 확인합니다:

```bash
./.venv/bin/wedding-watch test-notify
```

📱 폰에 알림이 오면 성공입니다. 안 오면 토픽 이름 철자를 다시 확인하세요.

## 4단계 — 로그인 한 번 하기 (5분) ⭐ 제일 중요

```bash
./.venv/bin/wedding-watch discover
```

**브라우저 창이 자동으로 열립니다.** 여기서 하실 일은 딱 두 가지입니다:

1. **결혼도움방에 로그인**하세요 (평소처럼)
2. **삼성금융연수원 예약 달력을 열고, 2027년 5월 → 6월 → 9월** … 이렇게 **월을 몇 번 넘겨보세요**

> 왜 월을 넘겨야 하나요?
> 프로그램이 "달력을 볼 때 사이트에 어떻게 물어보는지"를 옆에서 지켜보고 배웁니다.
> 몇 번 넘겨봐야 규칙을 정확히 파악합니다. **5~6번** 정도 넘겨주세요.

다 하셨으면 **터미널로 돌아와서 Enter** 를 누르세요. 브라우저는 저절로 닫힙니다.

이런 화면이 나오면 성공입니다:

```
 config.yaml 에 자동으로 설정을 써 넣었습니다.
   주소      : https://s-wedding.samsungcard.com/...
   날짜 필드 : WEDG_YMD
   자동 로그인: 켬
```

제대로 됐는지 확인합니다:

```bash
./.venv/bin/wedding-watch check --dry-run
```

지금 예약 가능한 날짜들이 쭉 나오면 **완벽합니다.**
(하나도 안 나와도 정상일 수 있습니다 — 정말로 빈자리가 없는 것일 수 있으니까요.)

## 5단계 — 감시 시작

```bash
./.venv/bin/wedding-watch run
```

이제 끝입니다. **이 터미널 창을 닫지 마세요.** 닫으면 감시가 멈춥니다.
창은 그냥 뒤로 밀어두시면 됩니다.

10분마다 이런 줄이 한 줄씩 올라갑니다:

```
2026-09-22 14:30:12 INFO  다음 확인: 14:40:33
```

빈자리가 나오면 **📱 딩동!**

---

## 알림은 이렇게 옵니다

| 알림 | 무슨 뜻인가요 | 뭘 해야 하나요 |
|---|---|---|
| 🎉 **빈자리 2건** | 예약 가능한 날짜가 새로 나왔습니다 | **당장** 결혼도움방 들어가서 예약! |
| ⚠️ **확인 실패 3회 연속** | 뭔가 막혔습니다 (보통 비밀번호가 바뀐 경우) | 아래 "문제가 생기면" 참고 |
| ✅ **감시 복구됨** | 문제가 저절로 해결됐습니다 | 아무것도 안 하셔도 됩니다 |

---

## 자주 묻는 것

**컴퓨터를 꺼도 되나요?**
안 됩니다. 컴퓨터가 켜져 있고 터미널 창이 떠 있어야 감시합니다. 노트북은 덮개를 닫아도 멈춥니다.
(맥: 시스템 설정 → 잠자기 없음 으로 두면 덮개를 닫아도 계속 돕니다.)

**터미널 창을 실수로 닫았어요**
다시 열고 아래 두 줄이면 됩니다. 그동안 알린 내역은 기억하고 있어서 같은 알림이 다시 오지 않습니다.
```bash
cd wedding-watch
./.venv/bin/wedding-watch run
```

**알림이 너무 자주 와요**
같은 날짜는 한 번만 알립니다. 자주 온다면 실제로 자리가 계속 나고 있다는 뜻입니다.
주말만 받고 싶으시면 `./.venv/bin/wedding-watch setup` 을 다시 실행하고 "주말만" 에 `y` 라고 답하세요.

**날짜를 바꾸고 싶어요 (예: 2028년으로)**
`./.venv/bin/wedding-watch setup` 을 다시 실행하시면 됩니다. 4단계(로그인)는 다시 안 하셔도 됩니다.

**1분마다 확인하면 더 빨리 잡지 않나요?**
그러면 사이트에 부담을 주고 차단될 수 있습니다. 10분이면 충분합니다 —
예약 취소가 나오고 10분 안에 다른 사람이 채가는 일은 드뭅니다. (1분 미만은 아예 막아뒀습니다.)

**다른 홀도 볼 수 있나요?**
네. `setup` 에서 홀 이름과 홀 코드를 바꾸고, 4단계를 그 홀 달력에서 다시 하시면 됩니다.

---

## 문제가 생기면

**⚠️ "확인 실패" 알림이 왔어요**

대부분 비밀번호 문제입니다. 순서대로 해보세요:

1. 결혼도움방에 **브라우저로 직접 로그인**해 보세요. 안 되면 비밀번호가 바뀐 겁니다
2. 바뀌었다면 `./.venv/bin/wedding-watch setup` 을 다시 실행해 새 비밀번호를 넣으세요
3. `./.venv/bin/wedding-watch test-login` 으로 확인하세요

> 비밀번호가 틀리면 프로그램은 **즉시 멈춥니다**. 10분마다 틀린 비밀번호로 계속 시도하면
> 계정이 잠기기 때문에 일부러 그렇게 만들었습니다.

**빈자리가 분명히 있는데 알림이 안 와요**

4단계에서 사이트가 달력을 어떻게 불러오는지 제대로 못 배웠을 수 있습니다.
`./.venv/bin/wedding-watch discover` 를 다시 실행하고, 이번엔 **월을 더 여러 번(8~10번)** 넘겨보세요.

**그래도 안 되면**

아래 명령의 결과를 복사해서 물어보시면 원인을 찾을 수 있습니다:

```bash
./.venv/bin/wedding-watch check --dry-run -v
```

> ⚠️ 결과에 이름·사번·연락처 같은 개인정보가 섞여 있으면 지우고 공유하세요.

---

## 그만두고 싶을 때

터미널 창에서 **`Ctrl + C`** 를 누르면 멈춥니다.
완전히 지우려면 `wedding-watch` 폴더를 통째로 삭제하시면 됩니다. (비밀번호도 같이 지워집니다.)
